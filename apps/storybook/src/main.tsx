import React from "react"
import ReactDOM from "react-dom/client"

import "@rightbrainai/ui/global.css"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <h1>@rightbrainai/storybook</h1>
  </React.StrictMode>
)
